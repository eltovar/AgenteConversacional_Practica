# Nixpacks configuration for Railway
# Asegura que ffmpeg con libopus esté disponible para conversión de audio → OGG/Opus

[phases.setup]
aptPkgs = ["ffmpeg", "libopus0", "libopus-dev"]

# No sobrescribir la detección automática de Python