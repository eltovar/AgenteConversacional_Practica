# Core LangChain
langchain==0.3.27
langchain-core==0.3.74
langchain-community==0.3.18
langchain-text-splitters==0.3.9

# NumPy - versión compatible con ambos langchain-community y langchain-postgres
numpy<2.0,>=1.26.0

# OpenAI (versión compatible con langchain-openai)
openai>=1.86.0
langchain-openai==0.3.29

# Pydantic (para Tools)
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Utilities
python-dotenv>=1.0.0
requests>=2.32.0

# FastAPI y servidor (para webhooks)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
gunicorn>=21.2.0
python-multipart>=0.0.6  # Requerido para Form() data de Twilio


# Redis (para persistencia de estado)
redis>=5.0.0

# PostgreSQL + pgvector (para RAG vectorial)
# Forzar instalación de dialectos SQLAlchemy para PostgreSQL
langchain-postgres==0.0.12
psycopg[binary]==3.2.3

# Testing (opcional)
pytest>=8.0.0
